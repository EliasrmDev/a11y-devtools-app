name = "a11y-devtools-dashboard"
pages_build_output_dir = "dist"


# ── Production environment variables ─────────────────────────────────────────
# VITE_* vars are baked into the bundle at build time — they must be present
# when `npm run build` runs, not at runtime.
#
# For local terminal deploy:  copy .env.example → .env.local and fill values.
# For Cloudflare Pages CI:    set in Dashboard → Settings → Environment variables.
[vars]
VITE_API_URL = "https://api.a11y.eliasrm.dev"
VITE_CLERK_PUBLISHABLE_KEY="pk_live_Y2xlcmsuYXBpLmExMXkuZWxpYXNybS5kZXYk"
# VITE_CLERK_PUBLISHABLE_KEY must be set via the CF Pages dashboard (secret)
