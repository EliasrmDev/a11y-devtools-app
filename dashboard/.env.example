# Backend API URL
VITE_API_URL=https://api.a11y.eliasrm.dev

# Frontend auth provider: `clerk` (default) or `neon`
VITE_AUTH_PROVIDER=clerk

# Clerk publishable key
VITE_CLERK_PUBLISHABLE_KEY=

# Neon Auth URL (used when VITE_AUTH_PROVIDER=neon)
VITE_NEON_AUTH_URL=
